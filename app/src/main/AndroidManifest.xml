<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.onecore.sdk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    
    <!-- Future Android 17-18 Permissions -->
    <uses-permission android:name="android.permission.VIRTUAL_DISPLAY" />
    <uses-permission android:name="android.permission.INTERCEPT_SYSTEM_CALLS" />
    <uses-permission android:name="android.permission.MANAGE_VIRTUAL_DEVICE" />
    
    <!-- Android 15 AVF Permission -->
    <uses-permission android:name="android.permission.MANAGE_VIRTUAL_MACHINE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:label="OneCore SDK">

        <activity android:name="com.onecore.sdk.core.SandboxActivity" 
                  android:process=":sandbox" 
                  android:exported="false"
                  android:screenOrientation="portrait"
                  android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Testing Activity for Social Login -->
        <activity android:name=".SocialLoginDemo" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <!-- Twitter Callback -->
                <data android:scheme="onecore.oauth" android:host="callback" />
                
                <!-- Facebook Callback (Replace {APP_ID} with real ID in production) -->
                <data android:scheme="fbYOUR_APP_ID" />
                
                <!-- Google Callback (Replace {CLIENT_ID} with real ID in production) -->
                <data android:scheme="com.googleusercontent.apps.YOUR_CLIENT_ID" />
            </intent-filter>
        </activity>
    </application>

</manifest>
