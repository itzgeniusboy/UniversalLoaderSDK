<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.onecore.sdk.container">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    
    <!-- Virtualization Permissions -->
    <uses-permission android:name="android.permission.VIRTUAL_DISPLAY" />
    <uses-permission android:name="android.permission.INTERCEPT_SYSTEM_CALLS" />
    <uses-permission android:name="android.permission.MANAGE_VIRTUAL_DEVICE" />
    <uses-permission android:name="android.permission.MANAGE_VIRTUAL_MACHINE" />

    <application
        android:label="OneCore SDK">

        <!-- Sandbox Host Process -->
        <activity android:name="com.onecore.sdk.core.SandboxActivity" 
                  android:process=":sandbox" 
                  android:exported="false"
                  android:screenOrientation="portrait"
                  android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- Stub Activity (Must be in same process as Sandbox for Hook persistence) -->
        <activity android:name="com.onecore.sdk.core.StubActivity"
                  android:process=":sandbox"
                  android:exported="false"
                  android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
                  android:configChanges="orientation|screenSize|keyboardHidden" />

        <service android:name="com.onecore.sdk.core.SandboxHeartbeatService"
                 android:process=":sandbox"
                 android:exported="false" />

        <service android:name="com.onecore.sdk.core.StubService"
                 android:process=":sandbox"
                 android:exported="false" />

        <activity android:name=".LauncherActivity" android:exported="true" android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TestActivity" android:exported="true" />
        
        <provider
            android:name=".core.provider.VirtualFileProvider"
            android:authorities="${applicationId}.virtual.fileprovider"
            android:exported="true"
            android:multiprocess="true"
            android:grantUriPermissions="true" />

        <activity android:name=".SocialLoginDemo" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="onecore.oauth" android:host="callback" />
            </intent-filter>
        </activity>
    </application>

</manifest>
