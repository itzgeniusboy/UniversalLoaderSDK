<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.onecore.sdk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <application
        android:theme="@style/Theme.OneCore"
        android:label="OneCore Loader"
        android:icon="@drawable/ios_glass_bg">

        <activity android:name="com.onecore.loader.SplashActivity" android:exported="true" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.onecore.loader.MainActivity" android:screenOrientation="portrait" />
        <activity android:name="com.onecore.loader.SettingsActivity" android:screenOrientation="portrait" />
        <activity android:name="com.onecore.loader.StatusActivity" android:screenOrientation="portrait" />
        
        <activity android:name="com.onecore.sdk.core.SandboxActivity" 
                  android:process=":sandbox" 
                  android:screenOrientation="portrait" />

        <!-- Testing Activity for Social Login -->
        <activity android:name=".SocialLoginDemo" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <!-- Twitter Callback -->
                <data android:scheme="onecore.oauth" android:host="callback" />
                
                <!-- Facebook Callback (Replace {APP_ID} with real ID in production) -->
                <data android:scheme="fbYOUR_APP_ID" />
                
                <!-- Google Callback (Replace {CLIENT_ID} with real ID in production) -->
                <data android:scheme="com.googleusercontent.apps.YOUR_CLIENT_ID" />
            </intent-filter>
        </activity>
    </application>

</manifest>
